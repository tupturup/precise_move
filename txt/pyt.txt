
___________________________________________

for target, p in enumerate(target_data):
        format_str = """INSERT INTO tbl_target (value_x, value_y, value_z, tbl_name)
        VALUES ({x}, '{y}', '{z}', '{t}');"""

        sql_command = format_str.format(x=p[0], y=p[1], z=p[2], t = p[3])
        print(sql_command)
        cursor.execute(sql_command)

    connection.commit()
    cursor.close()
    connection.close()
